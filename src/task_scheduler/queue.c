#include <stdio.h>
#include "queue.h"

void enqueue(void) {
    int insert_item;

    if (rear == SIZE - 1)
        printf("Queue overflow\n");
    else {
        if (front == -1) {
            front = 0;
            
        }
    }
}